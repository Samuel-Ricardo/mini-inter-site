import React, {StrictMode} from 'react';
import ReactDOM from 'react-dom';
//import App from '.app';

ReactDOM.render(
  <StrictMode>
    <div>Pedro Sites</div>
  </StrictMode>,
  document.getElementById('root')
)
