import { TextTheme } from "@Types/theme/text";

const H1: TextTheme = { FONT_SIZE: `16pt` };
const H2: TextTheme = { FONT_SIZE: `16pt` };
const P: TextTheme = { FONT_SIZE: `16pt` };
const LINK: TextTheme = { FONT_SIZE: `16pt` };
const TEXT: TextTheme = { FONT_SIZE: `16pt` };

export const global_fonts = {
  FONT_FAMILY: `'Roboto', sans-serif`,
  H1,
  H2,
  P,
  LINK,
  TEXT
};
